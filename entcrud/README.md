# entcrud

`entcrud` is a specialized code generator for creating CRUD (Create, Read, Update, Delete) operations for Ent schemas.

## Features

- Automatic generation of CRUD service implementations
- Integration with Ent models
- Support for common database operations

## Usage

Refer to the specific subpackage documentation for usage examples.
